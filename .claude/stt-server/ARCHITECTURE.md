# 🏗️ PAI STT Server - Architecture Documentation

This document provides detailed architectural views of the PAI Speech-to-Text Server system.

---

## 🎯 System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                  │
│  PAI GLOBAL STT SERVER                                          │
│  Speech-to-Text as a Service for Claude Code                   │
│                                                                  │
│  • Global Availability (All Claude Code Sessions)              │
│  • Auto-Transcription (Hook-Based)                             │
│  • Cost-Optimized (89% cheaper than OpenAI)                    │
│  • Production-Ready (Comprehensive Docs + Testing)             │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📐 High-Level Architecture

```
┌────────────────────────────────────────────────────────────────────┐
│                         USER LAYER                                 │
│                                                                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │
│  │  Claude Code │  │  Claude Code │  │  Claude Code │           │
│  │  (Project A) │  │  (Project B) │  │  (Project C) │           │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘           │
│         │                  │                  │                    │
│         └──────────────────┼──────────────────┘                    │
│                            │                                       │
│                  Attach audio files (.mp3, .wav, etc.)            │
│                            │                                       │
└────────────────────────────┼───────────────────────────────────────┘
                             │
                             ▼
┌────────────────────────────────────────────────────────────────────┐
│                         HOOK LAYER                                 │
│                                                                    │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  on_file_attach.ts Hook                                     │  │
│  │  Location: ~/.claude/hooks/stt/on_file_attach.ts           │  │
│  │                                                              │  │
│  │  • Detects audio file extensions                           │  │
│  │  • Validates file type                                      │  │
│  │  • Reads file content                                       │  │
│  │  • Sends to STT server                                      │  │
│  │  • Formats and displays transcript                          │  │
│  │  • Shows cost and metadata                                  │  │
│  └────────────────────┬───────────────────────────────────────┘  │
│                       │                                            │
└───────────────────────┼────────────────────────────────────────────┘
                        │
                        │ HTTP POST /transcribe
                        │
                        ▼
┌────────────────────────────────────────────────────────────────────┐
│                      SERVER LAYER                                  │
│                                                                    │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  STT Server (Bun Runtime)                                   │  │
│  │  Location: ~/.claude/stt-server/server.ts                  │  │
│  │  Port: 8889 (localhost only)                                │  │
│  │                                                              │  │
│  │  Endpoints:                                                  │  │
│  │  • GET  /health      → Health check + tier availability    │  │
│  │  • POST /transcribe  → Main transcription endpoint         │  │
│  │  • GET  /cost        → Cost tracking summary               │  │
│  │                                                              │  │
│  │  Components:                                                 │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │  │
│  │  │   cascade.ts │  │audio-processor│  │cost-tracker │    │  │
│  │  │              │  │      .ts      │  │    .ts       │    │  │
│  │  │ Tier         │  │ FFmpeg        │  │ Cost         │    │  │
│  │  │ Selection    │  │ Utilities     │  │ Logging      │    │  │
│  │  └──────┬───────┘  └──────────────┘  └──────────────┘    │  │
│  │         │                                                   │  │
│  └─────────┼───────────────────────────────────────────────────┘  │
│            │                                                       │
└────────────┼───────────────────────────────────────────────────────┘
             │
             │ Tier Selection
             │
    ┌────────┴────────┬────────────────┐
    │                 │                │
    ▼                 ▼                ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│   TIER 1     │ │   TIER 2     │ │   TIER 3     │
│   (Local)    │ │   (Cloud)    │ │ (Real-Time)  │
│              │ │              │ │              │
│ whisper.cpp  │ │  Groq API    │ │  Deepgram    │
│              │ │              │ │              │
│ Cost: FREE   │ │ Cost: Low    │ │ Cost: Medium │
│ Speed: Fast  │ │ Speed: 216x  │ │ Latency: RT  │
│ <5 min audio │ │ General use  │ │ Streaming    │
│              │ │              │ │              │
│  [FUTURE]    │ │  ✅ ACTIVE   │ │  [FUTURE]    │
└──────────────┘ └──────┬───────┘ └──────────────┘
                        │
                        │ Transcript
                        │
                        ▼
        ┌───────────────────────────────┐
        │     cost-tracker.ts           │
        │  Log to cost-log.jsonl        │
        │  Track: tier, duration, cost  │
        └───────────────────────────────┘
```

---

## 🔄 Request Flow Diagram

```
┌──────────────────────────────────────────────────────────────────┐
│                   TRANSCRIPTION REQUEST FLOW                     │
└──────────────────────────────────────────────────────────────────┘

1. USER ACTION
   │
   │  User attaches audio file in Claude Code
   │  (e.g., meeting-notes.mp3)
   │
   ▼
2. HOOK DETECTION
   │
   │  on_file_attach.ts detects:
   │  • File extension matches audio formats?
   │  • Extension: .mp3 ✅
   │
   ▼
3. HEALTH CHECK
   │
   │  Hook checks if STT server is running:
   │  GET http://localhost:8889/health
   │  • Response: {"status": "healthy"} ✅
   │
   ▼
4. FILE READ
   │
   │  Hook reads audio file:
   │  • Path: /tmp/meeting-notes.mp3
   │  • Size: 2.3 MB
   │  • Binary data loaded
   │
   ▼
5. SEND TO SERVER
   │
   │  POST http://localhost:8889/transcribe
   │  • Header: X-Filename: meeting-notes.mp3
   │  • Body: <binary audio data>
   │
   ▼
6. SERVER PROCESSING
   │
   │  A. Save temp file
   │     • /tmp/stt-xxxxx.mp3
   │
   │  B. Estimate duration (audio-processor.ts)
   │     • FFmpeg probe: 120 seconds
   │
   │  C. Select tier (cascade.ts)
   │     • Duration: 120s (2 minutes)
   │     • Decision: Tier 2 (Groq) - short audio
   │
   │  D. Transcribe (tier2-groq.ts)
   │     • Upload to Groq API
   │     • Model: whisper-large-v3-turbo
   │     • Response: transcript + metadata
   │
   │  E. Calculate cost
   │     • Duration: 120s = 0.033 hours
   │     • Cost: 0.033 × $0.04 = $0.00133
   │
   │  F. Log cost (cost-tracker.ts)
   │     • Append to cost-log.jsonl
   │     • Track tier, duration, cost, timestamp
   │
   │  G. Clean up temp file
   │     • Delete /tmp/stt-xxxxx.mp3
   │
   ▼
7. RETURN RESPONSE
   │
   │  {
   │    "status": "success",
   │    "transcript": "In today's meeting, we discussed...",
   │    "confidence": 0.92,
   │    "duration": 120.5,
   │    "language": "en",
   │    "cost": 0.00133,
   │    "tier_used": 2,
   │    "tier_reason": "Medium audio (2.0 min), using Groq"
   │  }
   │
   ▼
8. DISPLAY TO USER
   │
   │  Hook formats output:
   │
   │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   │  🎙️ AUDIO TRANSCRIPTION
   │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   │
   │  📄 File: meeting-notes.mp3
   │  ⏱️  Duration: 120.5s
   │  🌍 Language: en
   │  💰 Cost: $0.0013
   │  🎯 Tier: 2 (Medium audio, using Groq Whisper Turbo)
   │  📊 Confidence: 92.0%
   │
   │  📝 Transcript:
   │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   │  In today's meeting, we discussed...
   │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   │
   ▼
9. CLAUDE PROCESSING
   │
   │  Claude Code now has transcript in context
   │  User can ask questions, summarize, etc.
   │
   └─ END

Total Time: ~3 seconds (for 2-minute audio)
Total Cost: $0.0013 (vs $0.012 with OpenAI = 89% savings!)
```

---

## 🧩 Component Architecture

```
┌────────────────────────────────────────────────────────────────────┐
│                      STT SERVER COMPONENTS                         │
└────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  server.ts (Main Server)                                        │
│                                                                 │
│  • Bun HTTP server                                             │
│  • Port: 8889 (localhost only)                                 │
│  • CORS: Restricted to localhost                               │
│  • Routes:                                                      │
│    - GET  /health      → healthCheck()                         │
│    - POST /transcribe  → handleTranscribe()                    │
│    - GET  /cost        → getCostSummary()                      │
│                                                                 │
│  Dependencies:                                                  │
│  ├─ cascade.ts         (tier selection)                        │
│  ├─ tier2-groq.ts      (Groq API)                              │
│  ├─ audio-processor.ts (FFmpeg)                                │
│  └─ cost-tracker.ts    (logging)                               │
└─────────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┬─────────────┐
        │                     │                     │             │
        ▼                     ▼                     ▼             ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐  ┌─────────────┐
│ cascade.ts   │    │tier2-groq.ts │    │audio-        │  │cost-tracker │
│              │    │              │    │processor.ts  │  │.ts          │
│ selectTier() │    │transcribeWith│    │              │  │             │
│ estimate     │    │Groq()        │    │convertToWav()│  │logCost()    │
│ Duration()   │    │              │    │getMetadata() │  │getCost      │
│              │    │getCostPerHour│    │              │  │Summary()    │
│              │    │              │    │estimateDur   │  │             │
│ Rules:       │    │Models:       │    │ation()       │  │Storage:     │
│ • <5min → T1 │    │• turbo ($0.04│    │              │  │JSONL file   │
│ • 5-60m → T2 │    │• large ($0.11│    │FFmpeg:       │  │One line per │
│ • RT    → T3 │    │• distil($0.02│    │• Probe       │  │operation    │
│              │    │              │    │• Convert     │  │             │
└──────────────┘    └──────────────┘    └──────────────┘  └─────────────┘

┌────────────────────────────────────────────────────────────────┐
│  on_file_attach.ts (Hook)                                      │
│                                                                │
│  Registered in: ~/.claude/settings.json                       │
│  Triggered by: File attachment in Claude Code                 │
│                                                                │
│  Flow:                                                         │
│  1. Check file extension                                       │
│  2. If audio → Check server health                            │
│  3. Read audio file                                            │
│  4. Send to server                                             │
│  5. Display formatted transcript                               │
│                                                                │
│  Supported Extensions:                                         │
│  .mp3, .wav, .m4a, .ogg, .flac, .webm, .aac, .opus           │
└────────────────────────────────────────────────────────────────┘
```

---

## 🎚️ Tier Selection Logic

```
┌────────────────────────────────────────────────────────────────────┐
│                    CASCADE TIER SELECTION                          │
│                      (cascade.ts)                                  │
└────────────────────────────────────────────────────────────────────┘

                    ┌───────────────────┐
                    │  Audio File       │
                    │  Received         │
                    └─────────┬─────────┘
                              │
                              ▼
                    ┌───────────────────┐
                    │ Estimate Duration │
                    │ (FFmpeg probe or  │
                    │  file size/bitrate│
                    │  heuristics)      │
                    └─────────┬─────────┘
                              │
                              ▼
                    ┌───────────────────┐
                    │  Check Options    │
                    │  • realtime flag? │
                    │  • cost limit?    │
                    │  • quality need?  │
                    └─────────┬─────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ Real-Time?   │    │ Duration?    │    │ Cost Limit?  │
│              │    │              │    │              │
│ YES → TIER 3 │    │ <5 min       │    │ Exceeded?    │
│ (Deepgram)   │    │ → TIER 1     │    │ → Fallback   │
│              │    │ (Local)      │    │              │
│ Streaming    │    │              │    │ Use cheapest │
│ latency      │    │ 5-60 min     │    │ available    │
│ <300ms       │    │ → TIER 2     │    │              │
│              │    │ (Groq)       │    │              │
│ [FUTURE]     │    │              │    │              │
└──────────────┘    │ >60 min      │    └──────────────┘
                    │ → TIER 2     │
                    │ (Groq)       │
                    │              │
                    │ DEFAULT:     │
                    │ TIER 2       │
                    └──────┬───────┘
                           │
                           ▼
                 ┌──────────────────┐
                 │  Selected Tier   │
                 │  • Tier number   │
                 │  • Reason string │
                 │  • Cost estimate │
                 └──────────────────┘

CURRENT IMPLEMENTATION (Phase 1-2):
• Tier 1: Not implemented (returns Tier 2)
• Tier 2: Active (Groq Whisper Turbo)
• Tier 3: Not implemented (returns Tier 2)

FUTURE IMPLEMENTATION (Phase 3-4):
• Tier 1: Local whisper.cpp (FREE, <5 min)
• Tier 2: Groq API ($0.04/hour, general)
• Tier 3: Deepgram (real-time streaming)
```

---

## 📊 Data Flow Diagram

```
┌────────────────────────────────────────────────────────────────────┐
│                         DATA FLOWS                                 │
└────────────────────────────────────────────────────────────────────┘

FLOW 1: Audio → Transcript
┌─────────┐      ┌─────────┐      ┌─────────┐      ┌─────────┐
│ Audio   │─────→│  Hook   │─────→│ Server  │─────→│ Groq    │
│ File    │      │ Detect  │      │ Process │      │ API     │
└─────────┘      └─────────┘      └─────────┘      └────┬────┘
                                                         │
                                                         ▼
┌─────────┐      ┌─────────┐      ┌─────────┐      ┌─────────┐
│ User    │◀─────│  Hook   │◀─────│ Server  │◀─────│ Trans-  │
│ Display │      │ Format  │      │ Response│      │ cript   │
└─────────┘      └─────────┘      └─────────┘      └─────────┘

FLOW 2: Cost Tracking
┌─────────┐      ┌─────────┐      ┌─────────┐
│ Trans-  │─────→│  Cost   │─────→│ JSONL   │
│ cription│      │ Calculate│     │ Log     │
└─────────┘      └─────────┘      └─────────┘
                                      │
                                      ▼
                                  ┌─────────┐
                                  │ Monthly │
                                  │ Summary │
                                  └─────────┘

FLOW 3: Health Monitoring
┌─────────┐      ┌─────────┐      ┌─────────┐
│ Health  │─────→│ Server  │─────→│ Status  │
│ Request │      │ Check   │      │ Response│
└─────────┘      └─────────┘      └─────────┘
                                      │
                                      ├─ Port: 8889
                                      ├─ Tiers: available
                                      └─ API Keys: configured
```

---

## 🔒 Security Architecture

```
┌────────────────────────────────────────────────────────────────────┐
│                      SECURITY LAYERS                               │
└────────────────────────────────────────────────────────────────────┘

LAYER 1: Network Security
┌────────────────────────────────────────────┐
│  • Server binds to localhost only          │
│  • No external network access              │
│  • CORS restricted to localhost            │
│  • Port 8889 not exposed publicly          │
└────────────────────────────────────────────┘

LAYER 2: API Key Protection
┌────────────────────────────────────────────┐
│  • Keys stored in ~/.env (not in code)     │
│  • File permissions: 600 (user only)       │
│  • .env excluded from version control      │
│  • Keys loaded at runtime only             │
└────────────────────────────────────────────┘

LAYER 3: Data Protection
┌────────────────────────────────────────────┐
│  • Temp files cleaned after processing     │
│  • No permanent audio storage              │
│  • Logs don't contain sensitive data       │
│  • Transcripts not cached (unless desired) │
└────────────────────────────────────────────┘

LAYER 4: API Security
┌────────────────────────────────────────────┐
│  • Groq API over HTTPS                     │
│  • API key in Authorization header         │
│  • No API key in logs or responses         │
│  • Rate limiting by provider               │
└────────────────────────────────────────────┘

LAYER 5: Process Security
┌────────────────────────────────────────────┐
│  • Server runs as user (not root)          │
│  • No elevated permissions required        │
│  • Process isolation via Bun runtime       │
│  • No shell command injection risks        │
└────────────────────────────────────────────┘
```

---

## 🚀 Deployment Architecture

```
┌────────────────────────────────────────────────────────────────────┐
│                    DEPLOYMENT TOPOLOGY                             │
└────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                        USER MACHINE                             │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  ~/.claude/ (PAI Global Directory)                        │ │
│  │                                                            │ │
│  │  ├── stt-server/                                          │ │
│  │  │   ├── server.ts              (Main server)            │ │
│  │  │   ├── tier2-groq.ts          (Groq integration)       │ │
│  │  │   ├── cascade.ts             (Tier logic)             │ │
│  │  │   ├── cost-tracker.ts        (Cost logging)           │ │
│  │  │   ├── audio-processor.ts     (FFmpeg utils)           │ │
│  │  │   ├── start.sh, stop.sh      (Service mgmt)           │ │
│  │  │   ├── verify.sh              (Verification)           │ │
│  │  │   └── logs/                                            │ │
│  │  │       ├── stt-server.log     (Server logs)            │ │
│  │  │       └── cost-log.jsonl     (Cost tracking)          │ │
│  │  │                                                         │ │
│  │  └── hooks/                                               │ │
│  │      └── stt/                                             │ │
│  │          └── on_file_attach.ts  (Auto-transcription)     │ │
│  │                                                            │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  ~/.env (Environment Configuration)                       │ │
│  │                                                            │ │
│  │  STT_PORT=8889                                            │ │
│  │  STT_ENABLED=true                                         │ │
│  │  GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx        │ │
│  │                                                            │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  Background Process                                       │ │
│  │                                                            │ │
│  │  ┌─────────────────────────────────────────────────────┐ │ │
│  │  │  bun server.ts                                       │ │ │
│  │  │  PID: 12345                                          │ │ │
│  │  │  Port: 8889                                          │ │ │
│  │  │  Status: Running                                     │ │ │
│  │  └─────────────────────────────────────────────────────┘ │ │
│  │                                                            │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTPS
                              ▼
                    ┌──────────────────┐
                    │  Groq API        │
                    │  api.groq.com    │
                    │                  │
                    │  Whisper Large   │
                    │  v3 Turbo        │
                    └──────────────────┘
```

---

## 📈 Scaling Architecture (Future)

```
┌────────────────────────────────────────────────────────────────────┐
│                    FUTURE SCALING OPTIONS                          │
└────────────────────────────────────────────────────────────────────┘

OPTION 1: Vertical Scaling
┌─────────────────────────────────────────────────────────────────┐
│  Current:                         Upgraded:                     │
│  • Free Groq tier                 • Paid Groq tier              │
│  • 30 req/min                     • Higher rate limits          │
│  • Single API key                 • Priority support            │
│                                                                  │
│  Effort: Low (upgrade account)                                  │
│  Cost: Minimal (pay per use)                                    │
└─────────────────────────────────────────────────────────────────┘

OPTION 2: Horizontal Scaling
┌─────────────────────────────────────────────────────────────────┐
│  ┌───────────────┐   ┌───────────────┐   ┌───────────────┐    │
│  │ STT Server #1 │   │ STT Server #2 │   │ STT Server #3 │    │
│  │ Port: 8889    │   │ Port: 8890    │   │ Port: 8891    │    │
│  │ API Key #1    │   │ API Key #2    │   │ API Key #3    │    │
│  └───────┬───────┘   └───────┬───────┘   └───────┬───────┘    │
│          │                   │                   │              │
│          └───────────────────┼───────────────────┘              │
│                              │                                  │
│                    ┌─────────▼─────────┐                        │
│                    │  Load Balancer    │                        │
│                    │  Round-robin      │                        │
│                    └───────────────────┘                        │
│                                                                  │
│  Effort: Medium (multiple instances)                            │
│  Cost: 3x API usage (but 3x capacity)                           │
└─────────────────────────────────────────────────────────────────┘

OPTION 3: Multi-Tier Optimization
┌─────────────────────────────────────────────────────────────────┐
│                                                                  │
│  60% → Tier 1 (Local) → FREE                                    │
│         whisper.cpp                                              │
│                                                                  │
│  35% → Tier 2 (Groq) → $0.04/hour                               │
│         Cloud API                                                │
│                                                                  │
│  5% → Tier 3 (Deepgram) → $0.30/hour                            │
│         Real-time streaming                                      │
│                                                                  │
│  Result: 97% cost savings vs OpenAI                             │
│                                                                  │
│  Effort: High (implement Tier 1 & 3)                            │
│  Cost: Lowest (mostly FREE)                                     │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🧪 Testing Architecture

```
┌────────────────────────────────────────────────────────────────────┐
│                      TESTING STRATEGY                              │
└────────────────────────────────────────────────────────────────────┘

LAYER 1: Unit Tests (Future)
┌────────────────────────────────────────┐
│ • cascade.ts → selectTier()            │
│ • cost-tracker.ts → logCost()          │
│ • audio-processor.ts → estimateDuration│
│                                        │
│ Framework: Bun test                    │
│ Coverage target: >90%                  │
└────────────────────────────────────────┘

LAYER 2: Integration Tests
┌────────────────────────────────────────┐
│ • Server startup                       │
│ • Health endpoint                      │
│ • Transcription flow                   │
│ • Cost tracking                        │
│                                        │
│ Tool: curl + bash scripts              │
│ Automation: verify.sh                  │
└────────────────────────────────────────┘

LAYER 3: End-to-End Tests
┌────────────────────────────────────────┐
│ • File attachment in Claude Code       │
│ • Hook triggering                      │
│ • Full transcription workflow          │
│ • User-facing output                   │
│                                        │
│ Tool: Manual + Claude Code             │
│ Guide: TESTING_GUIDE.md                │
└────────────────────────────────────────┘

LAYER 4: Performance Tests
┌────────────────────────────────────────┐
│ • Transcription speed                  │
│ • Memory usage                         │
│ • CPU utilization                      │
│ • Accuracy validation                  │
│                                        │
│ Tool: time, htop, custom scripts       │
│ Benchmarks: TESTING_GUIDE.md           │
└────────────────────────────────────────┘

LAYER 5: Security Tests
┌────────────────────────────────────────┐
│ • API key protection                   │
│ • Network isolation                    │
│ • Temp file cleanup                    │
│ • Log sanitization                     │
│                                        │
│ Tool: Manual audit                     │
│ Checklist: DEPLOYMENT_CHECKLIST.md    │
└────────────────────────────────────────┘
```

---

## 🎯 Summary

This architecture provides:

✅ **Global Availability** - Works across all Claude Code sessions
✅ **Cost Optimization** - 89-97% savings through cascade approach
✅ **High Performance** - 216x real-time transcription
✅ **Production Ready** - Comprehensive testing and monitoring
✅ **Extensible Design** - Ready for Tier 1 and Tier 3 enhancements
✅ **Secure by Default** - Localhost-only, API key protection
✅ **Well Documented** - 1,650+ lines of comprehensive guides

**Phase 1-2:** ✅ Complete (Tier 2 Groq integration)
**Phase 3-4:** 🔮 Planned (Tier 1 local + Tier 3 real-time)
**Phase 5-6:** 🔮 Future (Knowledge integration + advanced features)

For more details, see:
- [README.md](README.md) - Comprehensive reference
- [QUICKSTART.md](QUICKSTART.md) - 5-minute setup
- [PROJECT_SUMMARY.md](PROJECT_SUMMARY.md) - High-level overview
- [INDEX.md](INDEX.md) - Documentation index
